# Use different secrets so participants cannot access the admin exports.
PARTICIPANT_PASSCODE=audit
ADMIN_PASSCODE=admin

# Existing deployments may keep a single PASSCODE instead. Both roles fall
# back to PASSCODE when their role-specific variable is unset.
# PASSCODE=legacy-shared-passcode
